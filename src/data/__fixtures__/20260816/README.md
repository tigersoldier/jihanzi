# 20260816 事故数据回归夹具

同步事故（2026-08-16 导出）的真实数据裁剪：

- `snapshot.json`：`snapshot_current.json` 原文——快照 SM-2 状态被同一复习
  重复应用约 3 次（interval 指数放大至 36500 封顶、nextReview 推到 2085/2126 年）。
- `logs.jsonl`：全部区间日志文件（`log_*.jsonl`）拼接——日志已被历史修复
  去重干净，与快照不一致。

## 数据错误摘要

| 类别 | 数量 | 代表字 | 说明 |
|------|------|--------|------|
| ×3 重复应用 | 130 个可验证字 | 今（reps 9→2、interval 21362→8） | 旧版同步把整份日志在已物化快照上重复重放 |
| 缺失应用 | 及/末 等 | 及（08-04 的 d 从未应用） | 旧版重放按墙钟过滤把新条目漏掉 |
| 机械重复 | 147+ 条 | 贯（03-03/03-04 同评级、timestamp 差 24h） | 同一复习被复制为次日条目 |
| 双设备重复 | 75 条 | 弯察示压仪（08-04/08-05 两次 round-1） | 两个客户端用分叉状态各自复习同一批字 |
| 8a 兜底目标 | 29 个字 | 伴/便/刻/途…（interval ≥ 10 年且历史不完整） | 日志缺早期复习，无法验证，只能重置重学 |

## 重新生成

上游数据变化时（导出目录 `/home/tiger/work/jihanzi/data/20260816/`）：

```bash
cp <导出目录>/snapshot_current.json src/data/__fixtures__/20260816/snapshot.json
cat <导出目录>/log_*.jsonl > src/data/__fixtures__/20260816/logs.jsonl
```

重新生成后需同步更新 `20260816-fixture.test.ts` 中的期望值（修复集合、
8a 兜底集合、关键字的守卫重放值）。
